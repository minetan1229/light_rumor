# Task: 【Phase 6】撮影現場支援計器・数学スタッキング・カラーチェッカー・プロマスター出力

## 1. 目的 & ゴール
USB-Cテザー撮影やシネマ級スコープを備えた**「撮影現場支援計器」**、星景や深度合成を行う**「オフライン数学スタッキング」**、24色カラーチェッカーによる**「カメラキャリブレーション」**、そして印刷用ICCソフトプルーフやExif電子透かしを統合した**「プロマスター出力」**を実装し、究極の現像スタジオを完成させます。

---

## 2. 必須技術スタック & 制約
* **ターゲット環境**: Android (Google Pixel 9a 最適化) ＋ PC (デスクトップビルド)
* **USB-C テザー通信**: Android USB Host API / libgphoto2 (PTP/IPプロトコル)
* **カラーマネジメント**: LittleCMS (lcms2) によるICCプロファイル色域変換
* **スタッキングアルゴリズム**: 特徴点マッチング（ORB / AKAZE）＋ ホモグラフィ幾何学アライメント

---

## 3. 実装要件（Field Tools, Stacking & Master Export Specifications）

### ① 撮影現場支援計器（Field Assistance Tools）
* **USB-C テザー撮影（Tethered Shooting）**:
  * 一眼カメラ（Sony, Canon, Nikon, Fuji等）とPixel 9a / PCをUSB-Cケーブルで直結。
  * カメラのシャッターを切った瞬間にRAWファイルを自動転送し、0.5秒以内に現像プリセットを自動適用して大画面プレビュー。
* **フォルスカラー（False Color - シネマカメラ標準露出ヒートマップ）**:
  * 画面全体の露出レベルをカラーヒートマップで可視化（紫=超アンダー、緑/ピンク=適正肌色、赤=白飛びクリッピング）。露出ミスを現場でゼロ化。
* **ゼブラパターン（Zebra Stripes）**:
  * 白飛び警戒エリア（IRE 95%〜100%で任意設定）に斜線ストライプをオーバーレイ表示。
* **フォーカスピーキング（Focus Peaking）**:
  * ピントが合っているエッジ部を蛍光色（赤・黄・青・白）でハイライト。マニュアルフォーカスのピント山を一目で把握。
* **プロフェッショナル波形スコープ**:
  * **波形モニター（Waveform）**: 画面全体の輝度分布を水平方向と連動してグラフ化。
  * **RGBパレード（RGB Parade）**: R/G/B各チャンネルの露出バランスを3列並列表示。
  * **ベクトルスコープ（Vectorscope）**: 色相と彩度の偏りを円形カラー空間上にプロット。

### ② オフライン数学スタッキング合成（Computational Stacking）
※AIではなく、純粋な幾何学・幾何統計アルゴリズムによる合成：
* **フォーカススタッキング（深度合成 / Focus Stacking）**:
  * ピント位置の異なる複数RAWから、ラプラシアンピラミッドにより最もシャープな領域を検出して合成。手前から奥まで全域にピントが合ったパンフォーカス画像を生成。
* **星景写真アライメント合成（Astro Stacking）**:
  * 地球の自転で移動する星の配置を幾何学的に星点マッチングし、星の位置を自動整列させて加算平均。超高感度撮影のノイズを極限まで低減。
* **NDフィルタ不要・長時間露光シミュレーション**:
  * 連写したRAW（10〜50枚）を中間値（Median）合成。NDフィルタなしで滝や波を滑らかにし、動いている通行人を自動消去。
* **多重露光（Multiple Exposure）**:
  * 複数画像のレイヤーブレンド（加算、平均、スクリーン、比較明、比較暗）＋手ブレ補正微小シフト。
* **超解像ピクセルシフト合成**:
  * カメラのセンサーシフトRAW（各社対応）を展開・合成し、偽色のない極限解像度画像を生成。

### ③ 24色カラーチェッカー測定 & プロファイル生成
* 撮影現場で写し込んだ24色カラーチェッカー（X-Rite / Calibrite）の枠を画面上で指定。
* 各パッチの色度（L*a*b*）を自動サンプリングし、光源環境に合わせた精密なカスタムカラープロファイル（DCP / ICC）を即座に生成・適用。

### ④ 印刷・プロマスター出力（Soft Proofing & Master Export）
* **ICCプロファイル・ソフトプルーフ（Soft Proofing）**:
  * 写真印刷用紙（キャノン、エプソン、ハーネミューレ等）のICCプロファイルとレンダリングインテント（知覚的 / 相対的）を読み込み、印刷時の色沈みや色域外警告（Gamut Warning）を画面上で完全再現。
* **マルチレシピ一括書き出し（Multi-Export Recipes）**:
  * 1回のタップで「印刷用16bit TIFF（AdobeRGB）」「Web用最高品質WebP（sRGB）」「SNS用長辺2048px縮小＋専用シャープネス適用JPEG」を同時並行書き出し。
* **Exif自動電子透かし印字（Metadata Watermark）**:
  * 写真の余白または端に、撮影時の「焦点距離、F値、シャッタースピード、ISO感度、カメラ/レンズ名、著作権」を洗練されたミニマルフォントで自動印字。

---

## 4. 出力成果物（生成すべきコード）
1. **テザー & 計器 (C++ & Vulkan)**:
   * `TetherManager.cpp`: USB Host API / PTPプロトコルによるRAW自動転送制御。
   * `field_scopes.comp`: フォルスカラー、ゼブラ、フォーカスピーキング、波形、ベクトルスコープ生成シェーダー。
2. **スタッキングエンジン (C++20)**:
   * `FocusStacker.cpp`: ラプラシアン深度ブレンドエンジン。
   * `AstroAligner.cpp`: 星点検出と幾何学アライメント加算平均エンジン。
3. **カラーマネジメント & 出力 (C++ & Kotlin)**:
   * `ColorCheckerCalibration.cpp`: 24色チャートサンプリングとプロファイル生成。
   * `SoftProofingManager.kt`: LittleCMS連携ICCソフトプルーフ制御。
   * `MultiExportRecipeManager.kt`: 一括レシピ並行エクスポート＆Exif電子透かしレンダラー。

---

## 5. 動作査定基準（Acceptance Criteria）
* [ ] USB-Cで接続した一眼カメラのシャッターを切った瞬間、自動で写真が読み込まれ現像画面に表示されること。
* [ ] フォルスカラーをONにした際、白飛びエリアが赤、適正露出エリアが緑に色分け表示されること。
* [ ] 5枚のピント位置違い写真を深度合成した際、手前から奥まで全体にピントが合った写真が生成されること。
* [ ] ソフトプルーフをONにした際、選択した用紙プロファイルに応じた色味の変化と色域外警告が確認できること。
* [ ] 一括書き出しを実行した際、Exif透かし入りのJPEG、WebP、16bit TIFFが指定フォルダに同時生成されること。
