# Task: 【Phase 1】画像入出力・RAW & 汎用画像現像・高画質エクスポートエンジン

## 1. 目的 & ゴール
プロフェッショナル写真現像アプリ「Project: APEX FIELD」の基幹である「画像入出力・現像・エクスポートエンジン」を構築します。
各社RAW画像（4,500万〜6,000万画素）に加え、**スマートフォンや一般的なカメラで撮影された非RAW画像（JPEG, PNG, HEIC/HEIF, WebP, TIFF 8bit/16bit）**もすべて同一の32bitリニア浮動小数点パイプラインに読み込み、メモリ不足（OOM）を防ぐタイル分割処理を経て、最高画質でストレージへ出力・保存できることを実機で査定・検証します。

---

## 2. 必須技術スタック & 制約
* ターゲット環境: Android (Google Pixel 9a 最適化 / ARM64) ＋ PC (Windows / macOS / Linux デスクトップビルド)
* 実行環境: 完全オフライン（ネットワーク通信なし）
* コア言語: C++20 (Android NDK) または Rust
* 画像デコード対応フォーマット:
  * RAW形式: LibRaw統合（Sony ARW, Canon CR2/CR3, Nikon NEF, Fuji RAF, DNG等の全社RAW完全対応）
  * 非RAW形式: libjpeg-turbo (JPEG), libpng (PNG), libheif (HEIC/HEIF), libwebp (WebP), libtiff (TIFF 8/16bit)
* 現像演算: Vulkan Compute Shader による高速ハードウェアアクセラレーション（CPUフォールバック完備）
* 内部演算色空間: 32bit リニア浮動小数点（Linear Rec.2020 / ACEScg / OKLab）
* UI/連携層: Kotlin (Android JNI経由) / AndroidX Foreground Service

---

## 3. 実装要件（Output Engine Specifications）

### ① 全フォーマット統一の32bitリニアインポート
* RAW画像はLibRawでデモザイク後、リニア色空間へ展開。
* JPEG/PNG/HEIC等の非RAW画像も、ガンマ解除（逆OETF）を行い、32bitリニア浮動小数点テクスチャとしてパイプラインに投入。
* 非RAW画像であっても、RAWと同様に白飛び復元、シャドウリフト、トーンカーブ、8色カラーミキサーなどの全現像パラメータを階調破綻なく適用可能とする。

### ② タイル分割レンダリング（OOM完全回避アーキテクチャ）
* 巨大な画像を一度にGPU/RAMに展開せず、2048 × 2048 ピクセルのタイル（チャンク）に分割して順次GPU現像パイプラインを通過させる。
* タイル境界でエッジノイズやノイズ除去の継ぎ目が出ないよう、適切なオーバーラップ（パディング）処理を行う。
* これにより、Pixel 9aなどの端末でもRAM消費量を数百MB以内に抑え、1億画素クラスの画像でも安定して書き出せること。

### ③ フル現像パイプライン（32bit Float Compute Shader）
入力画像（RAWおよび非RAW）に対し、以下の全パラメータを32bitリニア空間で正確に適用するシェーダー/演算ロジックを実装すること：
1. ホワイトバランス & 色被り補正: 色温度（Kelvin）＋色被り（Tint）＋シャドウ色被りの行列演算。
2. 基本トーン & 自然な彩度: 露光量、コントラスト、ハイライト/シャドウ復元、白/黒レベル、自然な彩度（肌色保護Vibrance）、彩度。
3. トーンカーブ: 1D LUTテクスチャによるスプラインサンプリング。
4. 8色カラーミキサー & モノクロ現像: 8色HSL色調整、および8チャンネル白黒ミキサー（光学フィルターシミュレーション）。
5. ディテール & ノイズ削減: 輝度ノイズ低減、カラー偽色ノイズ低減、マスキング付きエッジシャープネス。

### ④ 色空間変換 & ディザリング（Banding & Color-shift Prevention）
* ICC色空間トーンマッピング:
  * 作業色空間（Linear）から出力先色空間（sRGB / Display P3 / AdobeRGB）への正確な伝達関数（OETF）適用。
  * プレビューと書き出し結果で色のくすみ・ズレが絶対に起きない設計。
* 高精度ディザリング（Dithering）:
  * 32bit浮動小数点から8bit/16bitへ量子化する際、三角形確率密度関数（TPDF）等のランダムディザを適用し、滑らかな空などのバンディング（階調の縞模様）を完全防止。

### ⑤ ファイルエンコード & メタデータ保持
* 対応出力フォーマット:
  * JPEG (8bit): 品質1〜100指定、クロマサブサンプリング（4:4:4最高画質 または 4:2:0軽量）。
  * TIFF (16bit / 8bit): 完全非圧縮またはLZW可逆圧縮。
  * WebP: ロスレス / 不可逆圧縮。
  * Linear DNG: 16bit浮動小数点リニアRAW形式。
* Exif / IPTC メタデータの完全引き継ぎ:
  * 元のファイルから撮影日時、カメラモデル、レンズ名、露出設定（F値、SS、ISO）、GPS座標を抽出。
  * 出力先ファイルのExifヘッダーにそれらを完全再注入して書き出す。

### ⑥ バックグラウンド書き出し & 進捗通知
* Androidの Foreground Service を実装し、通知バーに進捗率（0〜100%）を表示。
* ユーザーがアプリをバックグラウンドに回したり画面を消しても、OSにタスクキルされずに安全に出力を完遂すること。

---

## 4. 出力成果物（生成すべきコード）
1. Native Core (C++/NDK):
   * RawDecoder.cpp: LibRawを使用したRAWファイルの読み込み・デモザイク・タイル切り出し。
   * StandardImageDecoder.cpp: JPEG/PNG/HEIC/WebP/TIFFの32bitリニア展開デコーダー。
   * ExportPipeline.cpp & Compute Shader: 32bitリニアでのトーン/色補正/ノイズ低減/ディテール補正、色空間変換、ディザリング処理。
   * ImageWriter.cpp: libjpeg-turbo / libtiff を用いたExif付き高画質エンコーダー。
2. JNI ブリッジ:
   * Kotlinから現像パラメータ、入出力ファイルパス、進捗コールバックを受け渡すJNIバインディング。
3. Android Kotlin Service:
   * ExportService.kt: Foreground Serviceによる安全なバックグラウンド実行と通知表示。
4. 動作確認・査定用テスト実行コード:
   * RAW画像および一般的なJPEG/PNG画像を渡し、補正を施して保存先に最高画質JPEGおよび16bit TIFFとして正常に出力されることを検証するテスト関数。

---

## 5. 動作査定基準（Acceptance Criteria）
* [ ] RAWファイル（DNG/ARW等）だけでなく、一般的なJPEGやHEIC、PNG画像も正常に読み込まれ、現像処理が行えること。
* [ ] 指定した保存先フォルダに、実際に開くことができるJPEGおよびTIFFが生成されること。
* [ ] 生成されたJPEGの青空や暗部に縞模様（バンディング）がなく、指定した露出・WB・色被り・自然な彩度が正しく反映されていること。
* [ ] 生成されたファイルのプロパティで、元の撮影情報（Exif）が正しく引き継がれていること。
* [ ] 高解像度画像を書き出してもメモリ使用量が急上昇せず、OOMクラッシュしないこと。
