# Task: 【Phase 5】超拡張手動マスク・レタッチ・XMPプリセット・無制限リール巻き戻し

## 1. 目的 & ゴール
AIに頼らず写真家が意図通りに光と色を操る**「9種類の超拡張局所マスクシステム」**、**「コピースタンプ・手動修復レタッチ」**、Lightroom互換の**「XMPプリセット管理」**、そして失敗を恐れず何度でも過去に戻れる**「アナログリール風無制限巻き戻し（Undo/Redo）エンジン」**を実装・査定します。

---

## 2. 必須技術スタック & 制約
* **ターゲット環境**: Android (Google Pixel 9a 最適化) ＋ PC (デスクトップビルド)
* **マスク描画**: Vulkan Compute Shader によるマルチレイヤー合成（レイヤー数 10枚以上でも60fps維持）
* **スタイラス対応**: Android MotionEvent による筆圧感知（Pressure sensitivity）
* **プリセット形式**: Adobe Lightroom XMP 形式完全互換（XML / YAML パーサー）
* **状態管理**: 不変データ構造（Immutable State Tree）による履歴管理

---

## 3. 実装要件（Masking, Retouch, Presets & History Specifications）

### ① 9種類の超拡張手動マスクシステム（Local Masking Engine）
すべてのマスクは独立したレイヤーとして多重生成でき、各マスクごとに全トーン、WB、明瞭度、カーブ、色調を個別に適用可能：
1. **線形グラデーション（Linear Gradient）**: 2点間の直線減衰、フェザー幅、イージング曲線（リニア/S字）、反転。
2. **円形・楕円グラデーション（Radial/Elliptical）**: 中心点、長軸/短軸、回転角、フェザー（ぼかし幅）、内外半径。
3. **多角形・ベジェパス（Polygon & Bezier）**: 精密アンカーポイント曲線による幾何学切り抜き。
4. **精密フリーハンドブラシ（Brush）**:
   * スタイラス筆圧感知（筆圧によるサイズ/濃淡変化）、サイズ、フェザー、流量（Flow）、密度（Density）。
   * 消しゴムモード（Eraser）完備。
5. **輝度範囲マスク（Luminance Range）**:
   * ヒストグラム上で特定の明るさ帯域（0〜255）を指定。ハイライト/シャドウ境界のスムーズ減衰スライダー。
6. **カラー範囲マスク（Color Range）**:
   * スポイトで画面上の任意の色を選択。色相（Hue）、彩度（Sat）、明度（Lum）の許容差（Tolerance）スライダー。
7. **深度マップマスク（Depth Map）**:
   * スマートフォンカメラ（ToF / 視差）が記録した深度データを解析し、物理的な距離（近景/中景/遠景）で分離。
8. **エッジ・輪郭マスク（Sobel/Laplacian）**:
   * 輪郭線のみを自動抽出してマスク化。
9. **マスクのブール演算合成**:
   * 各マスクレイヤー間で**「加算（Union）」「減算（Subtract）」「交差（Intersect）」「反転（Invert）」**を実行可能。
   * 例: 「円形グラデーション」から「輝度マスクの暗部」を減算し、被写体の顔のハイライトだけを照らす。

### ② コピースタンプ & 手動修復ブラシ（Healing & Clone）
* **コピースタンプ（Clone Stamp）**: 画面上の別領域（ソース元）のテクスチャをそのままピクセル転送。
* **手動修復ブラシ（Heal）**: ポアソン画像合成（Poisson Image Editing）により、転送元テクスチャの明度と色味を周囲にシームレスに馴染ませてゴミやセンサーダストを除去。

### ③ XMPプリセット管理 & 適用量スライダー
* **Lightroom XMPプリセット完全互換**:
  * `.xmp` 形式のプリセットファイルを直接インポートおよびエクスポート。
  * プリセットのカテゴリ分類（風景、ポートレート、フィルム、モノクロ等）。
* **プリセット適用量スライダー（Amount: 0%〜200%）**:
  * プリセットの全調整値を一括で薄めたり（50%）、強調（150%）できるスライダー。
* **リアルタイムホバープレビュー**:
  * プリセット一覧をタップする前に、指を置いただけでプレビューが即座に切り替わる高速プレビュー。

### ④ アナログリール風無制限巻き戻し（Undo / Redo / History）
* **不変スナップショット履歴**:
  * パラメータ変更のたびに差分スナップショットを生成し、メモリ内にDAGグラフとして保持。
* **アナログリールUI**:
  * 画面下部にアナログフィルムのリールを回すようなUIを配置。指でリールを回すと、10手前、50手前の編集状態に瞬時にタイムトラベル。
* **履歴ツリー分岐（Snapshot Branching）**:
  * 過去の地点に戻って別の編集を始めた場合でも、以前の履歴が消えず、新しいブランチ（枝分かれ）として両方のバリエーションを保存・切り替え可能。

---

## 4. 出力成果物（生成すべきコード）
1. **Vulkan マスクパイプライン (GLSL & C++)**:
   * `mask_evaluation.comp`: 9種のマスク生成およびブール演算（Union/Subtract/Intersect）シェーダー。
   * `poisson_heal.comp`: センサーダスト修復用ポアソンブレンドシェーダー。
2. **プリセット & 状態エンジン (Kotlin)**:
   * `XmpPresetParser.kt`: Lightroom XMPファイルのパース・シリアライズエンジン。
   * `HistoryManager.kt`: 不変DAGツリーによる無制限Undo/Redoとブランチ管理。
3. **Compose UI コンポーネント**:
   * `MaskLayerManager.kt`: マスクレイヤー一覧、追加、反転、ブール演算設定パネル。
   * `AnalogReelHistoryBar.kt`: 触覚フィードバック付きアナログリール巻き戻しバー。
   * `PresetBrowser.kt`: 適用量スライダー付きプリセットセレクター。

---

## 5. 動作査定基準（Acceptance Criteria）
* [ ] 円形グラデーションから輝度マスクを「交差（Intersect）」させ、特定領域のハイライトだけに補正がかかること。
* [ ] スタイラスペンで筆圧を弱めると細く薄く、強めると太く濃くブラシマスクが描けること。
* [ ] 外部から読み込んだLightroom用 `.xmp` プリセットが正確に適用され、適用量スライダーで濃淡が操作できること。
* [ ] アナログリールを回して50手前の状態に戻した際、1ミリ秒の遅延もなく過去の絵が完全に再現されること。
