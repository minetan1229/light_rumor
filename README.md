# light_rumor - 次世代プロフェッショナルRAW現像・写真編集スタジオ

[![Build & Release APK](https://github.com/minetan1229/light_rumor/actions/workflows/build-and-release.yml/badge.svg)](https://github.com/minetan1229/light_rumor/actions/workflows/build-and-release.yml)
[![Release](https://img.shields.io/github/v/release/minetan1229/light_rumor?color=FF7900&label=APK%20Release)](https://github.com/minetan1229/light_rumor/releases/latest)

---

## ダウンロード
### ⬇️ [**light_rumor v1.1.5 をダウンロード（APK）**](https://github.com/minetan1229/light_rumor/raw/master/dist/light_rumor-v1.1.5.apk)

**最新バージョン: v1.1.5 更新内容**
- **C++ ネイティブエンジンの安全性 & 堅牢性の抜本的強化**:
  - **45MP 超高解像度 RAW の 32-bit オーバーフロー根絶**: `RawDecoder`, `DenoiseEngine`, `MaskEngine` におけるピクセル積算やコピーサイズ計算を `size_t` / `int64_t` に全面移行し、巨大画像処理時のヒープ破壊・未定義動作を完全防止。
  - **規格外 WebP 出力の廃止 & 安全ハンドリング**: JPEG 偽装ビットストリーム出力を廃止し、破損ファイルを生成しない安全なエラーハンドリングに変更。
  - **TIFF16 出力時のバッファ保護**: IFD タグ StripOffsets 書き戻し時に境界チェックを追加し、不正なメモリアクセスを遮断。
  - **JPEG 量子化テーブルの走査順整合**: 自然走査順と Zigzag 順の混在を解消し、画質クオリティに応じた整数テーブル計算を適正化。
  - **メモリバッファからの RAW 現像（`openBuffer`）完全展開**: `unpack()` 後の現像処理とリニア画像バッファ（FloatRGBA）への正規化転送を実装。
  - **JNI 参照リークの完全根絶（RAII ガード導入）**: `JniStringUtfGuard` および `JniGlobalRefGuard` を導入し、例外発生時や早期 return 時でも文字列および GlobalRef が確実に解放される構造に改善。
  - **エクスポートパイプラインのマルチスレッド競合解消**: 静的 `DenoiseEngine` を `thread_local` 化し、並行エクスポート時のデータ競合を排除。
  - **星景アライメント & フォーカススタックの境界保護**: RANSAC 星マッチングおよび OpenMP スレッドバッファの安全クランプを追加。
- **GLSL コンピュートシェーダーの Vulkan 規格適合 & 表示補正**:
  - **Push Constant 128バイト制限適合**: 構造体を 148B から 112B へ圧縮パッキングし、Pixel 9a を含む全 GPU 端末での動作を保証。
  - **構造体アライメント & バッファ参照の構文修正**: std140 規則に準拠したアライメントパディングの追加、および SSBO インスタンス名経由のアクセスに統一。
  - **アンシャープマスクの符号反転バグ修正**: ラプラシアン計算の符号を正しく修正し、輪郭がぼやける問題を解消してエッジのみを鮮鋭化。
  - **リニア出力・波形モニター・RGBパレード・コンポジットマスク実装**: リニア色空間出力（`cs == 0`）の早期リターン、波形モニター、RGBパレードの集計分岐、コンポジットマスク評価を追加。
- **Kotlin コア・サービス・データ管理の堅牢化**:
  - **ネイティブ未ロード時のクラッシュ防止**: ロード状態フラグ `isAvailable` と安全ラッパーを追加し、`UnsatisfiedLinkError` を防止。
  - **履歴ツリーのスレッド安全性 & 不変性確保**: `HistoryNode.childrenIds` を不変リスト化し、ノードのコピー更新による完全なイミュータブル DAG ツリーを実現。
  - **SharedPreferences のアトミック排他制御**: `@Synchronized` による排他制御および `Dispatchers.IO` で動作する非同期メソッドを整備。
  - **CoroutineScope メモリリーク防止**: 各マネージャーに `cancel()` メソッドを追加し、ライフサイクル終了時のリソース解放を徹底。
  - **ContentResolver 二重オープン解消 & 強制アンラップ排除**: 同一ファイルディスクリプタの seek による高速デコード、および `uri.path!!` / `intent.data!!` の安全 null ガード化。
  - **エクスポート失敗時のクリーンアップ**: 失敗時に中途半端な出力ファイルを自動削除。
- **Compose 状態検知 & UI 反応性の抜本的強化**:
  - **バーチャルノッチダイヤル（`PrecisionDial`）の差分検知**: `DialConfig` を不変データクラス化し、回転操作のリアルタイム再描画を確実にトリガー。
  - **マスクレイヤーの不変オブジェクト更新**: `layer.copy(...)` で新しい状態オブジェクトを生成し、Compose State を安全に更新。ブラシクリアの即時伝播を実現。
  - **選別スタジオ（Culling）の安全化**: プレビュー Bitmap の安全呼び出し化、および EXIF メタデータの不変更新への刷新。
  - **一括同期（BatchSync）のバックグラウンド IO 化**: メインスレッドのブロックとフリーズ（ANR）を防止。
  - **RGB 波形モニター（ColorWaveformView）のメモリ安全化**: メインスレッド更新および `DisposableEffect` での明示的な `Bitmap.recycle()` 呼び出し。
  - **プリセットブラウザのタッチ操作性改善**: `detectTapGestures` の Press/Release/Tap によるプレビュー表示・解除・確定適用の適正化。
  - **最新 Kotlin `Enum.entries` への全面移行**: UI 全域で非推奨の `values()` を廃止。
- **ビルド設定・セキュリティ・Manifest の強化**:
  - **リリース署名運用の安全化**: 環境変数が存在する場合のみ release 署名を設定し、未設定時は debug 署名にフォールバック。
  - **インテントフィルタ脆弱性の解消**: `VIEW` インテントから `BROWSABLE` と `file://` の危険な組み合わせを分離・解消。
  - **Android 14/15 ForegroundServiceType 整合性確保**: `ExportService` とのフォアグラウンドサービス型定義を完全整合。

**過去のバージョン: v1.1.4**
- **Compose 状態検知 & UI 反応性の抜本的強化**:
  - **バーチャルノッチダイヤル（`PrecisionDial`）の差分検知**: ダイヤル設定（`DialConfig`）を不変データクラス化し、ダイヤル回転時の Compose 状態再描画を確実にトリガー。
  - **LaunchedEffect キー最適化**: 写真オブジェクト全体ではなく `photoItem.uri` をキーに指定し、不要な再読み込みやプレビューのちらつきを排除。
  - **最新 Kotlin `Enum.entries` への全面移行**: `values()` による一時配列アロケーションを撤廃し、UI 全域で最新の `entries` プロパティへ移行。
- **局所調整マスク操作 & レイヤー不変状態管理**:
  - **マスクレイヤーの不変オブジェクト更新**: `MaskLayerState` の直接ミューテーションを撤廃し、`layer.copy(...)` で新しい状態オブジェクトを生成して Compose State を安全に更新。
  - **ブラシストローク全消去（クリア）の即時伝播**: 古いレイヤー参照ではなく最新のレイヤーリストを安全に伝播・永続化。
- **選別スタジオ（Culling）の安全化 & EXIF 不変更新**:
  - **強制アンラップの根絶**: プレビュー Bitmap の強制アンラップ（`!!`）を排除し、安全な null ガード呼び出しに修正。
  - **メタデータ更新の安全化**: 日時・カメラ・レンズ・F値・SS・ISO等の EXIF 直接変更箇所およびレーティング・採用フラグ変更を `metadata.copy(...)` による不変更新へ刷新。
- **一括同期（BatchSync）のバックグラウンド IO 化**:
  - パラメータ同期実行時の XMP 保存ループを `withContext(Dispatchers.IO)` に移行し、メインスレッド（UI スレッド）のブロックとフレームドロップを防止。
- **RGB 波形モニター（ColorWaveformView）のメインスレッド更新 & メモリ安全化**:
  - 計算完了後の波形 Bitmap 更新をメインスレッドで安全に行い、Composable 破棄時の `DisposableEffect` で明示的な `Bitmap.recycle()` を呼び出しメモリリークを根絶。
- **プリセットブラウザのタッチ操作性改善**:
  - タッチ端末においてホバープレビューが解除されなくなる問題を `detectTapGestures` の `onPress` / `tryAwaitRelease` / `onTap` により解決。押下中のリアルタイムプレビューと指離し時の確実な解除、タップ時の確定適用を両立。
- **ビルド設定・セキュリティ・Manifest の強化**:
  - **リリース署名運用の安全化**: `build.gradle.kts` で環境変数（`KEYSTORE_FILE` 等）が存在する場合のみ release 署名を設定し、安全な CI/CD およびローカルビルド環境を両立。
  - **NDK リンクライブラリ強化 & Include パス整理**: `CMakeLists.txt` の `target_link_libraries` に `android` を追加し、ターゲット固有の include ディレクトリを整理。
  - **Android 14/15 ForegroundServiceType 整合性確保**: `ExportService` のフォアグラウンドサービス型を Android 14 (API 34) と Android 15 (API 35) の OS 仕様・定数定義と完全に整合させ、実行時例外を回避。
  - **インテントフィルタ脆弱性の解消**: `VIEW` インテントフィルタから `BROWSABLE` と `file://` の危険な組み合わせを分離・解消し、外部からの不正なファイルアクセスを防御。また `EDIT` / `VIEW` の `<data>` タグを属性ごとに適切に分離。
  - **丸型アイコン指定**: Android 7.1+ 向けに `android:roundIcon="@mipmap/ic_launcher_round"` を指定。

**過去のバージョン: v1.1.3**
- **全現像パラメータの完全永続化 & データ消失の根絶**: カタログ（`EditHistoryCatalog`）の JSON 保存に HSL 各帯域、ノイズ除去、シャープネス、RGBキャリブレーション、スプリットトーニング、レンズ補正、ジオメトリを完全統合。アプリ復帰や次回起動時の設定消失を根絶。
- **ナビゲーション & システム操作性の向上**:
  - **システム「戻る」ボタン/ジェスチャー完全対応**: 現像スタジオ、マルチ比較画面、選別画面で `BackHandler` を統合。意図しないアプリ終了を防ぎ、直前の編集状態や画面へ確実に遷移。
  - **外部共有 Intent の受領安定化**: アプリ起動中に外部ギャラリーやファイルマネージャーから写真が共有された際にも、`onNewIntent` により即座に現像画面を展開。
- **UI 操作性 & レターボックス追従の精密化**:
  - **二重スクロール競合の解消**: カラーミキサー画面やディテール/ノイズ低減画面での入れ子スクロール競合を解消し、スライダー操作と縦スクロールの滑らかな連動を実現。
  - **水平出しルーラーの反転バグ修正**: クロップ・回転ツールで右から左へドラッグした際に回転角が 180° 反転してしまう問題を修正し、ドラッグ方向に自然に追従。
  - **写真実領域に完全追従するマスク UV 座標**: 写真のレターボックス/ピラーボックス実描画領域を数学的に算出（`calculateFittedRect`）し、スタイラス筆圧ブラシや各手動マスクの描画ズレを解消。
  - **プリセット選択時の画面フリッカー根絶**: タッチリリース時の過剰なプレビュー解除を防止し、タップ確定時のプレビューから適用へのシームレスな移行を実現。
  - **マルチ比較画面の描画境界保護**: 4画面連動等倍比較時、隣接ペインへの描画はみ出しを防ぐため境界クリッピング（`.clipToBounds()`）を適用。
- **エクスポート & OS ギャラリー即時同期**:
  - **エクスポートタスクの完全キューイング**: `AtomicInteger` によるタスクカウンタを導入し、複数写真の連続エクスポート要求時に後続タスクが破棄される問題を解消。すべてのエクスポートが順次確実に実行されます。
  - **タイル分割現像時のマスク・レタッチ座標完全追従**: 大解像度画像のタイル分割出力時、画像全体のグローバル座標系（`fullWidth, fullHeight, tileOriginX, tileOriginY`）を各タイルへ伝達。分割境界でのマスクズレ・レタッチ崩壊を根絶。
  - **MediaScanner 自動登録**: 現像エクスポート完了時およびマルチレシピ書き出し完了時に `MediaScannerConnection` を即座に呼び出し、Google フォト等の外部ギャラリーへ写真が即時反映。
  - **自動拡張子フォールバック**: ファイル名に拡張子が欠落している場合でも、選択フォーマット（JPEG/TIFF/DNG/WebP）に応じた適正な拡張子を自動補完。
  - **フォアグラウンド・エクスポートの ANR/クラッシュ防止**: `ExportService` の `startForeground` を起動直後に確実に呼び出し、Android 8.0+ の `ForegroundServiceDidNotStartInTimeException` を完全回避。
  - **マルチエクスポートのレシピ並行安全性**: 各エクスポートレシピ実行時にパラメータの独立性を担保。
- **XMP サイドカー XML 構文の修復 & 安全な非破壊運用**:
  - **XMP XML 構文修復 & 特殊文字エスケープ**: `<rdf:Description>` タグの閉じ括弧不整合を修正し、特殊文字のエスケープ処理を徹底。Adobe Lightroom 等との完全な相互運用性を確保。
  - **プロプライエタリ RAW ファイルの破損ガード**: `DirectExifWriter` で ARW/CR2/CR3/NEF 等の独自 RAW に対する危険な実ファイル直接バイナリ書き換えをスキップし、非破壊 XMP 運用を徹底。
- **Bitmap メモリ管理 & OOM クラッシュの根絶**: 波形モニター（`ColorWaveformView`）の旧 Bitmap 即時解放と Composable 破棄ハンドリング、`ThumbnailLoader` での `readBytes()` 廃止と `decodeSampledBitmapFromSource` による安全なダウンサンプリング、回転処理後の元 Bitmap 解放を徹底。
- **C++20 ネイティブエンジンの精度・安定性向上（Phase 1〜6 全テスト 100% PASS）**:
  - **JPEG ハフマン符号化の 64bit 拡張**: `JpegBitWriter` のビットバッファを `uint64_t` に拡張し、高周波写真で発生し得たハフマン符号化の 40bit オーバーフロー・画像破損を根絶。
  - **TIFF 8bit への Exif 埋め込み**: TIFF 8bit 出力時にも Exif メタデータを確実に埋め込み保存。
  - **星景追尾スタック（AstroAligner）幾何学マッチング**: 対向頂点インデックスの追従ソートを実装し、三角形幾何学星マッチングの精度を大幅向上。
  - **ピクセルシフト超解像（FocusStacker）**: 空間サンプリング座標オフセット（Shot 0〜3）を正確にマッピング。
  - **レンズプロファイル双方向マッチング（LensfunIntegration）**: レンズモデル名の双方向部分一致と焦点距離レンジ検証を強化。
  - **スポット修復（Poisson Heal）境界保護**: ROI 局所領域処理と画像境界のはみ出し保護・ゼロ除算ガードを追加し、画像端での黒塗り・クラッシュを防止。
  - **FieldScopes 18% グレー IRE 判定**: Rec.709 OETF ガンマ変換式を正確に適用し、ゼブラ・フォールスカラーの誤判定を防止。
  - **Defringe / SoftProofing / Tone 演算の数学的修正**: 色収差除去の適正な彩度減衰式、ペーパーホワイトシミュレーションの極性修正、Dehaze 負値処理、ハイライト抑制極性を修正。

**過去のバージョン: v1.1.2**
- **露出スライダー操作時のクラッシュ根絶**: 現像スタジオで露出（Exposure）スライダーを動かし始めた瞬間に発生していた強制終了を完全修正。
- **選別画面ヘッダーのレスポンシブ化 & 見切れ解消**: 狭い画面幅のスマートフォン（360〜412dp）でも写真ファイル名がボタンを押し出さないよう、ファイル名の安全な省略表示とアクションボタン行の横スクロール対応を実装。
- **選別画面に「+ 読み込み」ボタンを新設**: コレクション画面に戻ることなく、選別・現像中のセッションに直接追加で写真を読み込めるようになりました。
- **「最近の編集」履歴の完全復元 & 永続化**: 選別画面での評価や現像調整の内容、ファイルパスをカタログへ確実に二重保存。
- **「採用」タグ・星レーティング・カラーラベルの確実な保存 & サムネイルバッジ表示**: XMPサイドカーへの非同期書き出しに加え、アプリ内部カタログにも即時同期保存。
- **「すべて選択」ボタンの視認性向上**: コレクション画面のタブバーが画面幅に応じて適切にスクロールし、ボタンが常に確実に表示・操作できるよう調整。

**過去のバージョン: v1.1.1**
- **致命的バグ・安定性の抜本改善（全監査エラー修正）**:
  - **スポット修復（Poisson Heal）のメモリ最適化**: 全画面1.5GBバッファ確保をROI局所領域（数十KB）へ局所化し、実機端末でのOOMクラッシュを完全根絶
  - **現像編集値の完全保持＆UI再描画の正常化**: 現像スタジオからの復帰時に最新パラメータをカタログに確実に永続保存し、Compose `equals` の全比較によりRecompositionスキップを解消
  - **RGB波形モニター（Colors Wave）の赤・青色整合**: Android `ARGB_8888` 規格と整合させ、パレード表示の赤・青反転を修正
  - **EXIF・XMPサイドカーのロケール安全化**: `Locale.US` 明示により非英語圏での小数点カンマ化・XML構文破損リスクを解消
  - **JNI・C++20境界安全性の強化**: サムネイル抽出のnull安全化、タイル境界外アクセス・フレーム長検証を徹底
- **端末情報・カメラ機種・レンズ・EXIFの自由変更＆実ファイル直接書き込み保存**
- **スライダー操作性の完全修復（ThumbZone）**
- **写真上での直観的マスク操作＆「欠けたところ」色分け表示**
- **カラープロファイル実写サムネイル＆モノクロ統合**
- **端末写真「コレクション」タブ＆まとめて一括選択**

Android 8.0（API 26）以降、**全6フェーズのプロフェッショナル機能すべてが使えます**。

**入れかた**

1. 上のリンクをスマホで開いて APK を保存する
2. 保存したファイルをタップする
3. 「提供元不明のアプリ」の許可を求められたら許可する
   （Android 8 以降はアプリごとの許可。ブラウザやファイル管理アプリに出す）
4. インストールして開く

> [!TIP]
> **完全オフライン動作**: 外部通信権限（インターネット権限）を一切持たず、RAW現像から色の波（RGB波形モニター）、9種の手動マスク、スタッキング合成まで端末ローカルで高速動作します。
>
> [!IMPORTANT]
> **先行配布版（APK直接共有）**: デバッグ鍵で署名されているため、端末のセキュリティ警告が出た場合は「続行 / インストール」を選択してください。将来 Google Play 版を導入する場合は一度アンインストールが必要になります。

---

## 概要

**light_rumor** は、Adobe Lightroom Classic、Capture One、DaVinci Resolveの強みを統合し、完全オフラインで動作する次世代RAW & 汎用画像現像・写真編集アプリケーションです。

RAW（ARW / CR3 / NEF / DNG等）だけでなく一般的なJPEG/PNG/HEIC画像への完全対応、ワンタップ写真許可Photo Picker、色の波（RGB波形モニター）、CSSアニメーション物理イージング、AI特有の安っぽいデザインの完全排除、トリミング・回転・ターゲット調整ツール・レンズデータベース・プリセット同期・テザー撮影・スタッキング・印刷ソフトプルーフまで、プロが求める全6フェーズの全機能を実装レベルで体系化しています。

---

## 開発思想 & 厳格な規範
1. **完全手動・プロフェッショナル制御**: AIによる自動補正やおまかせ機能に頼らず、すべての光・色彩・数学的合成を写真家自身がコントロール可能。
2. **AI風デザインの完全排除（Anti-AI Aesthetic）**: ネオングラデーション、無駄な角丸、グラスモーフィズム、キラキラアイコンを厳禁。ライカやハッセルブラッドのようなマットブラック（#0A0A0C）とチタングレーの硬派な測定計器デザインを徹底。
3. **CSSアニメーション & 物理イージング**: cubic-bezierによる機械的で吸い付くような操作モーションと120Hz追従。
4. **RAW & 汎用画像（JPEG/HEIC/PNG）の統一高画質現像**: スマホ撮影の非RAW写真も32bitリニア色空間で階調豊かに現像。
5. **超低摩擦な写真許可**: Android Photo Pickerにより、面倒なストレージ権限なしで写真を選ぶだけで即座に現像開始。
6. **シネマ級の計器「色の波」**: RGB波形モニター（Waveform / RGB Parade）で光と色の分布をリアルタイム可視化。
7. **絵文字の完全排除と実写サムネイルの採用**: UIおよびシステム全体で絵文字アイコンの使用を一切禁止。実写写真サムネイルおよび精密な計器グラフィックを採用。
8. **段階的実機査定（Phase-by-Phase Verification）**: 全6フェーズの査定基準（Acceptance Criteria）をネイティブテストで100%パス。
9. **完全オフライン & 速度最優先**: 通信を一切行わず、C++20 + Vulkan Compute Shaderによる32bit浮動小数点リニア演算で高速出力を維持。

---

## 全フェーズ実装機能一覧（全6フェーズ完了）

* [phase1.md](./phase1.md): 【Phase 1】画像入出力・RAW & 汎用画像現像・高画質エクスポートエンジン
  * RAW（ARW/CR3/NEF/DNG等）および非RAW（JPEG/HEIC/PNG/WebP/TIFF）対応、タイル分割レンダリング（OOM完全回避）、32bitリニア現像、色空間変換、TPDFディザリング、JPEG(4:4:4)/16bit TIFF/WebP/DNG出力、Exif完全保持、Foreground Service。
* [phase2.md](./phase2.md): 【Phase 2】OS即時起動Intent・Photo Pickerワンタップ許可・ゼロ遅延カリング・カタログ・バッチ同期
  * Photo Pickerによるワンタップ写真アクセス、撮影時/保存時の即時起動Intent、RAW内包JPEG非同期0msカリング、レーティング評価、4画面連動100%等倍ズーム比較、選択パラメータ一括同期（Batch Sync）。
* [phase3.md](./phase3.md): 【Phase 3】冒険写真家の計器デザイン・色の波（RGB波形モニター）・人間工学サムゾーンUI
  * AI風デザイン排除規範、CSSアニメーション物理イージング、色の波（リアルタイムRGB波形モニター / RGB Parade）、Obsidian Black（純黒#0A0A0C）＆ Arctic Minimal（白）、実写写真サムネイルUI、下部35%集約サムゾーン、フリック＋±0.01精密回転ダイヤル、ゼロ点触覚フィードバック（Haptics）、RGBヒストグラム直接ドラッグ、4種のBefore/After比較。
* [phase4.md](./phase4.md): 【Phase 4】トリミング・変形・全現像コア・60fps Vulkanパイプライン
  * 自由/固定アスペクト比クロップ（XPan等対応）、水平出しルーラー、反転、ガイド付き変形パース補正。
  * WB（Kelvin/Tint/スポイト）、原色キャリブレーション、基本トーン、自然な彩度、ガイドフィルタ明瞭度/テクスチャ/Dehaze。
  * 8色カラーミキサー（HSL）、本格モノクロ現像（実写フィルター作例サムネイル表示）。
  * 輝度ノイズ低減、カラーノイズ低減、マスキング付きシャープネス。
  * トーンカーブ（RGBマスター＋各色＋パラメトリック）＆写真直感ターゲット調整ツール（TAT）。
  * 3ウェイカラーホイール、3D LUT(.cube)、Lensfunレンズ補正。
* [phase5.md](./phase5.md): 【Phase 5】超拡張手動マスク・レタッチ・XMPプリセット・無制限リール巻き戻し
  * 9種の手動マスク（線形、円形/楕円、ベジェ/多角形、筆圧ブラシ、輝度、カラー、Depth、エッジ、ブール演算）。
  * コピースタンプ、手動テクスチャ修復ブラシ。
  * XMPプリセット完全互換インポート/エクスポート、実写プレビューサムネイル付きプリセットブラウザ、適用量スライダー（0〜200%）。
  * アナログリール風タイムラインUndo/Redo、履歴ツリー分岐。
* [phase6.md](./phase6.md): 【Phase 6】撮影現場支援計器・数学スタッキング・カラーチェッカー・プロマスター出力
  * USB-Cテザー撮影、フォルスカラー、ゼブラパターン、フォーカスピーキング、波形モニター/RGBパレード/ベクトルスコープ。
  * フォーカススタッキング（深度合成）、星景追尾スタック、ND不要長時間露光合成、ピクセルシフト超解像、多重露光。
  * 24色カラーチェッカー測定、ICCソフトプルーフ、マルチレシピ一括書き出し、Exif自動電子透かし印字。

---

## ビルド手順

### 1. Android APKビルド (Gradle)
```bash
./gradlew assembleRelease
# 生成先: app/build/outputs/apk/release/light_rumor.apk
```

### 2. デスクトップネイティブテスト実行 (C++20 / CMake)
```bash
cmake -B build -G Ninja
cmake --build build --target light_rumor_phase6_test
./build/light_rumor_phase6_test
```
